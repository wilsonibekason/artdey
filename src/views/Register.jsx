import React from "react";
import { AppRegister } from "../components/auth";
import Layout from "./Layout";

const Register = () => {
  return (
    <Layout>
      <AppRegister />
    </Layout>
  );
};

export default Register;
